/ RoleBased(all the models in one folder )
FeatuesBased() - src/
index.js // main server files
controllers/
middlewares/
services/
utils/ (which is going to be common for some files)
configs/
repository/ - tests[later]
